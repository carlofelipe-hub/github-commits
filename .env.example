# Classic GitHub personal access token (read-only: read:user, public_repo).
# Fine-grained PATs do NOT work with the GitHub GraphQL API.
GITHUB_TOKEN=ghp_your_token_here

# Optional
PORT=4000
CACHE_TTL_SECONDS=3600
